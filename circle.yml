general:
  branches:
    only:
      - master

machine:
  timezone: Europe/London
  node:
    version: 6.9.0

dependencies:
  override:
    - yarn

compile:
  override:
    - yarn build
  
deployment:
  production:
    branch: master
    commands:
      - yarn deploy
